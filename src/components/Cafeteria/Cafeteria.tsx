import React from 'react';
import Header from '../Header/Header';

const Cafeteria = () => {
    return (
        <>
            
            <Header />
        </>
    );
};

export default Cafeteria;